import type { NextConfig } from 'next'

// Posts are rendered server-side from posts/*.mdx via next-mdx-remote,
// so no extra MDX webpack loader is needed here.
const nextConfig: NextConfig = {}

export default nextConfig
